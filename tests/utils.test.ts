import { describe, test } from "bun:test";

describe("hexToBuff()", () => {
    test("should correctly convert hex to buffer", () => {
        const cases: [string, Buffer] = [
            
        ];
    });
});